public enum EventType
{
    VIEW, // hide or show output
    BLOCKED, // blocked selection, the output show but grey and not selectable
    CUSTOM, // call script
    MOVE, // move to other scene
    NONE
}